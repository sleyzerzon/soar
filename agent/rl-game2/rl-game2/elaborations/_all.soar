sp {elaborate*state*name
   (state <s> ^superstate.operator.name <name>)
-->
   (<s> ^name <name>)
}

sp {elaborate*state*top-state
   (state <s> ^superstate.top-state <ts>)
-->
   (<s> ^top-state <ts>)
}

sp {elaborate*io
   (state <s> ^superstate <ss>)
   (<ss> ^io <io>)
-->
   (<s> ^io <io>)}

sp {elaborate*player-ids
   (state <s> ^superstate <ss>)
   (<ss> ^my-pid <mpid>
         ^enemy-pid <epid>)
-->
   (<s> ^my-pid <mpid>
        ^enemy-pid <epid>)}