sp {cat-find-cog*propose*restore-grouping-radius
   (state <s> ^name cat-find-cog
              ^superstate.memory.cog <cog>
              ^grouping-radius-exploded t)
-->
   (<s> ^operator <o> +)
   (<o> ^name restore-grouping-radius)}

sp {cat-find-cog*apply*restore-grouping-radius
   (state <s> ^name cat-find-cog
              ^superstate <ss>
              ^io.output-link.sorts <cmd>
              ^grouping-radius-exploded <e>
              ^operator <o>)
   (<ss> ^cluster-radius <cr>)
   (<o> ^name restore-grouping-radius)
-->
   (<cmd> ^grouping-radius <gr-cmd>)
   (<gr-cmd> ^value <cr>)
   (<ss> ^find-cog-complete t)}