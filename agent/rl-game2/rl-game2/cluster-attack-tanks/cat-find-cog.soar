sp {cluster-attack-tanks*propose*find-cog
   (state <s> ^name cluster-attack-tanks
              ^instance <si>
             -^find-cog-complete
             -^tanks-clustered)
-->
   (<s> ^operator <o> + >)
   (<o> ^name cat-find-cog)}