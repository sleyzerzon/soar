sp {robot*propose*circuit-patrol
   (state <s> ^name robot
              ^current-mission <cm>)
   (<cm> ^type << patrol linear-patrol circuit-patrol >>
         ^first-area <fa>)
-->
   (<s> ^operator <op> + =)
   (<op> ^name patrol
         ^first-area <fa>)
}

sp {robot*propose*patrol
   (state <s> ^name robot
              ^current-mission <cm>)
   (<cm> ^name patrol
         ^first-area <fa>)
-->
   (<s> ^operator <op> + =)
   (<op> ^name patrol
         ^first-area <fa>)
}
