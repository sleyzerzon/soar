sp {circuit-patrol*propose*next-area
   (state <s> ^name patrol
              ^achieved-area true
              ^next-area.next <> nil)
-->
   (<s> ^operator <op> + =, >)
   (<op> ^name next-area)
}

sp {apply*next-area
   (state <s> ^operator.name next-area
              ^next-area <na>)
   (<na> ^next <next>)
-->
   (<s> ^next-area <na> -
                   <next>)
}
