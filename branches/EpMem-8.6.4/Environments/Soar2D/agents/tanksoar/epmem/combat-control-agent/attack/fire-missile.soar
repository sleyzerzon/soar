###Propose Fire Operator
##  If the state is attack and there is a tank on radar in the center,
##  then propose the fire missile operator. 

sp {attack*propose*fire-missile
   (state <s> ^name attack
              ^io.input-link <il>)
   (<il> ^radar.tank.position center
         ^missiles > 0)
   -->
   (<s> ^operator <o> +)
   (<o> ^name fire-missile
        ^actions.fire.weapon missile)}

###Added by :AMN: on 22 Aug 06
#sp {attack*propose*fire-missile*pot-shot
#   (state <s> ^name attack
#              ^io.input-link <il>)
#   (<il> ^radar.tank.position << left right >>
#         ^random > 0.9
#         ^missiles > 0)
#-->
#   (<s> ^operator <o> +)
#   (<o> ^name fire-missile
#        ^actions.fire.weapon missile)}
#
###Added by :AMN: on 22 Aug 06
#sp {attack*compare*fire-missile
#   (state <s> ^name attack
#              ^operator <op1> +
#                        <op2> +)
#   (<op1> ^name fire-missile)
#   (<op2> ^name << slide turn move >>)
#-->
#   (<s> ^operator <op1> > <op2>)
#}

