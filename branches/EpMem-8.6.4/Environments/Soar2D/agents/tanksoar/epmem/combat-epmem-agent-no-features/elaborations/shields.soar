#%%%13 Sep 06: Modified these rules to elaborate at proposal 
#              rather than post-decision
#sp {elaborate*shields-on
#   (state <s> ^operator <o> +
#              ^io.input-link <il>)
#   (<il> ^incoming.<dir> yes
#         ^shield-status off)
#   (<o> ^actions <a>)
#-->
#   (<a> ^shields.switch on)
#}

sp {elaborate*shields-off
   (state <s> ^operator <o> +
              ^io.input-link <il>)
   (<il> -^incoming.<dir> yes
          ^shield-status on)
   (<o> ^actions <a>)
-->
   (<a> ^shields.switch off)
}

