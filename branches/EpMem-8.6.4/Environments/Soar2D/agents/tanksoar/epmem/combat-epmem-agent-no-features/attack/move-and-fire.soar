sp {attack*propose*move-and-fire
   (state <s> ^name attack
              ^io.input-link.blocked.<dir> no)
-->
   (<s> ^operator <o> +)
   (<o> ^name move-and-fire
        ^actions <a>
        ^direction <dir>)
   (<a> ^move.direction <dir>
        ^fire.weapon missile)
}
