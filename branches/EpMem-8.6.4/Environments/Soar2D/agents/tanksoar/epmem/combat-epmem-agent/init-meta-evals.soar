echo
echo init-meta-evals

#IF there are no meta-evaluation links
#THEN propose creating and initializing them
sp {propose*init-meta-evals
   (state <s> ^name tanksoar
             -^pending-meta-evals)
-->
   (<s> ^operator <op> + =)
   (<op> ^name init-meta-evals)
}

#Init before taking actions in the world
sp {tanksoar*compare*init-meta-evals
   (state <s> ^name tanksoar
              ^operator <op1> +
                        <op2> +)
   (<op1> ^name init-meta-evals)
   (<op2> ^name << attack chase retreat wander >>)
-->
   (<s> ^operator <op1> > <op2>)
}
