soar=../../../../out/bin/s

socat unix-connect:env,retry=100 system:"java RobotClient" &
cpid=$!

while [ -n "$
if [ "$1" == "-g" ]
then
	gdb --args $soar robot.soar
else
	$soar robot.soar
fi

kill $cpid
javapid=`ps | awk '$4 == "java" {print $1}'`

if [ -n "$javapid" ]
then
	kill "$javapid"
fi
