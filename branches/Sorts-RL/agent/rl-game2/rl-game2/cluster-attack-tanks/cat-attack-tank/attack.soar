sp {cluster-attack-tanks*attack-base*propose*attack
   (state <s> ^name cat-attack-tank)
-->
   (<s> ^operator <o> +)
   (<o> ^name attack)}

sp {cluster-attack-tanks*attack-base*apply*attack
   (state <s> ^name cat-attack-tank
              ^superstate <ss>
              ^io.output-link.sorts <cmd-link>
              ^operator <o>)
   (<ss> ^my-tanks.id <mtid>
         ^enemy-tanks.id <etid>)
   (<o> ^name attack)
-->
   (<cmd-link> ^attack <a>)
   (<a> ^group0 <mtid>
        ^group1 <etid>)}