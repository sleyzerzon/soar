sp {cluster-attack-bases*propose*set-cluster-radius
   (state <s> ^name cluster-attack-bases
              ^memory <m>
             -^cluster-radius)
-->
   (<s> ^operator <o> +)
   (<o> ^name set-cluster-radius)}

sp {cluster-attack-bases*apply*set-cluster-radius
   (state <s> ^name cluster-attack-bases
              ^operator <o>)
   (<o> ^name set-cluster-radius)
-->
   (<s> ^cluster-radius 40)}

sp {cluster-attack-bases*apply*set-cluster-radius*set-grouping-radius
   (state <s> ^name cluster-attack-bases
              ^io.input-link.vision-state.grouping-radius <> 40
              ^io.output-link.sorts <cmd-link>
              ^operator <o>)
   (<o> ^name set-cluster-radius)
-->
   (<cmd-link> ^grouping-radius <gr-cmd>)
   (<gr-cmd> ^value 40)}