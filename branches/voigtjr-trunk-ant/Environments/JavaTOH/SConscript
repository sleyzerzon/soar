#!/usr/bin/python
import glob

Import('env')
env = env.Clone()

theJar = '#Environments/JavaTOH/JavaTOH.jar';
sources = glob.glob('src/mac/*')

env.Command(theJar, sources, 'ant', chdir=1)
env.Clean(theJar, '#Environments/JavaTOH/build')
env.Install('#SoarLibrary/bin', theJar)

